<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:vm="clr-namespace:SystemInfo.Core.ViewModels;assembly=SystemInfo.Core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary
            Source="..\Resources\SegmentedProgressBar.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <DataTemplate
        DataType="{x:Type vm:CPUViewModel}">
        <StackPanel>
            <DockPanel
                Margin="0,0,0,2">
                <TextBlock
                    Text="CPU: "
                    DockPanel.Dock="Left" />
                <TextBlock
                    Text="{Binding TotalUse, FallbackValue=0%, StringFormat='\{0:F0\}%', Mode=OneWay}"
                    TextAlignment="Right"
                    DockPanel.Dock="Right" />
            </DockPanel>
            <ItemsControl
                ItemsSource="{Binding CoreUses, Mode=OneWay}"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Top">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ProgressBar
                            Background="{Binding DataContext.WindowSettings.CpuProgressbarSettings.BackgroundColor, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                            Height="{Binding DataContext.WindowSettings.CpuProgressbarSettings.Height, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                            Value="{Binding Mode=OneWay}"
                            Style="{StaticResource segmentedBarStyle}">
                            <ProgressBar.Foreground>
                                <LinearGradientBrush
                                    EndPoint="1,0">
                                    <GradientStop
                                        Color="{Binding DataContext.WindowSettings.CpuProgressbarSettings.ForegroundColorStart, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                        Offset="0" />
                                    <GradientStop
                                        Color="{Binding DataContext.WindowSettings.CpuProgressbarSettings.ForegroundColorMiddle, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                        Offset="0.6" />
                                    <GradientStop
                                        Color="{Binding DataContext.WindowSettings.CpuProgressbarSettings.ForegroundColorEnd, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                        Offset="1" />
                                </LinearGradientBrush>
                            </ProgressBar.Foreground>
                        </ProgressBar>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>